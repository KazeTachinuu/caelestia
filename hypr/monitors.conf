# Monitor configuration handled by kanshi for automatic profile switching
# This file only contains fallback rules for unknown monitors

# Fallback: any unknown monitor gets auto-positioned
# (The default in hyprland.conf already handles this)
